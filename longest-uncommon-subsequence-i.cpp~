#include <iostream>
#include <stack>

class Solution {
public:
  int findLUSlength(std::string a, std::string b) {
    stack<char> s;
    int ans=0;
    bool flag = false;
    if(std:strcmp(a,b)==0)
      return = -1;
    else {
      if(a.length()>=b.length()) {
	for(char& c : a) {
	  
	}

      }
      else 
	findLUSlength(b,a);
    }
    
    


  }
};


int main(int argc, char**argv[])
{
  Solution sol = Solution();
  std::cout<<sol.findLUSlength("abc","cef");
}
